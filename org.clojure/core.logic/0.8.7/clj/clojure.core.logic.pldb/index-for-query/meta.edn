{:ns "clojure.core.logic.pldb", :name "index-for-query", :file "clojure/core/logic/pldb.clj", :type :fn, :src "(defn index-for-query [s q indexes]\n  (let [indexable (map #(ground? s %)  q)\n        triples (map vector (range) indexable indexes)]\n    (first (for [[i indexable indexed] triples\n                 :when (and indexable indexed)]\n             i))))", :column 1, :line 41, :arglists ([s q indexes])}