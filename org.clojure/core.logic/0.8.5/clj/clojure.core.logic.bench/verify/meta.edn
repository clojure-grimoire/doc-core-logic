{:ns "clojure.core.logic.bench", :name "verify", :file "clojure/core/logic/bench.clj", :type :fn, :src "(defn verify [vars]\n  (let [rows (->rows vars)\n        cols (->cols rows)\n        sqs  (->squares rows)\n        verify-group (fn [group]\n                       (every? #(= (->> % (into #{}) count) 9)\n                          group))]\n    (and (verify-group rows)\n         (verify-group cols)\n         (verify-group sqs))))", :column 1, :line 647, :arglists ([vars])}