{:ns "cljs.core.logic.macros", :name "extract-vars", :file "cljs/core/logic/macros.clj", :type :fn, :src "(defn extract-vars\n  ([p]\n     (set (cond\n           (lvar-sym? p) [p]           \n           (coll? p) (let [p (if (seq? p) (rest p) p)]\n                       (filter lvar-sym? (flatten p)))\n           :else nil)))\n  ([p seen]\n     (set/difference (extract-vars p) (set seen))))", :column 1, :line 260, :arglists ([p] [p seen])}