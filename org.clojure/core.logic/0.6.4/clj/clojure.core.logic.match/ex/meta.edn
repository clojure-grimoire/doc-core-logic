{:ns "clojure.core.logic.match", :name "ex", :file "clojure/core/logic/match.clj", :type :fn, :src "(defn ex\n  ([vs t a]\n     `(fresh [~@vs]\n        (== ~t ~a)))\n  ([vs t a exprs]\n     (if (fresh-expr? exprs)\n       `(fresh [~@vs]\n          (== ~t ~a)\n          ~exprs)\n       `(fresh [~@vs]\n          (== ~t ~a)\n          ~@exprs))))", :column 1, :line 47, :arglists ([vs t a] [vs t a exprs])}