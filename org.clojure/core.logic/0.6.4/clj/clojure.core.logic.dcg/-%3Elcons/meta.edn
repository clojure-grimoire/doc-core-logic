{:ns "clojure.core.logic.dcg", :name "->lcons", :file "clojure/core/logic/dcg.clj", :type :fn, :src "(defn ->lcons\n  ([env [m :as c] i] (->lcons env c i false))\n  ([env [m :as c] i quoted]\n     (cond\n      (empty? c) `(fresh []\n                    (== ~(env (dec i)) ~(env i)))\n      :else (let [m (if quoted `(quote ~m) m)]\n              `(== ~(env (dec i)) (lcons ~m ~(env i)))))))", :column 1, :line 21, :arglists ([env [m :as c] i] [env [m :as c] i quoted])}