{:ns "clojure.core.logic.nonrel", :name "project", :file "clojure/core/logic/nonrel.clj", :type :macro, :src "(defmacro project [[& vars] & goals]\n  \"Extract the values bound to the specified logic vars. Non-relational.\"\n  (let [a (gensym \"a\")]\n    `(fn [~a]\n       (let [~@(project-bindings vars a)]\n         ((fresh []\n            ~@goals) ~a)))))", :column 1, :line 17, :macro true, :arglists ([[& vars] & goals])}