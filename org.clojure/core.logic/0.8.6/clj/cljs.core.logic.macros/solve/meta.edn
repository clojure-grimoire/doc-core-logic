{:ns "cljs.core.logic.macros", :name "solve", :file "cljs/core/logic/macros.clj", :type :macro, :src "(defmacro solve [& [n [x] & goals]]\n  `(let [xs# (cljs.core.logic/-take* (-inc\n                      ((fresh [~x] ~@goals\n                         (fn [a#]\n                           (cljs.core.logic/-reify a# ~x))) ;; TODO: do we need this?\n                       cljs.core.logic/empty-s)))]\n     (if ~n\n       (take ~n xs#)\n       xs#)))", :column 1, :line 66, :macro true, :arglists ([& [n [x] & goals]])}