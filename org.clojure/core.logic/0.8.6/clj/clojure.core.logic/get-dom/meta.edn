{:ns "clojure.core.logic", :name "get-dom", :file "clojure/core/logic.clj", :type :fn, :src "(defn get-dom [s x dom]\n  (let [v (root-val s x)]\n    (cond\n      (subst-val? v) (let [v' (:v v)]\n                       (if (not= v' ::unbound)\n                         v'\n                         (-> v :doms dom)))\n      (not (lvar? v)) v)))", :column 1, :line 502, :arglists ([s x dom])}