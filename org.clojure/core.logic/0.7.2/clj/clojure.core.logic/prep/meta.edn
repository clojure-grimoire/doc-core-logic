{:ns "clojure.core.logic", :name "prep", :file "clojure/core/logic.clj", :type :fn, :src "(defn prep\n  \"Prep a quoted expression. All symbols preceded by ? will\n  be replaced with logic vars.\"\n  [expr]\n  (let [lvars (atom {})\n        prepped (if (lcons-expr? expr)\n                  (prep* expr lvars true)\n                  (postwalk (replace-lvar lvars) expr))]\n    (with-meta prepped {:lvars @lvars})))", :column 1, :line 1003, :arglists ([expr]), :doc "Prep a quoted expression. All symbols preceded by ? will\n  be replaced with logic vars."}