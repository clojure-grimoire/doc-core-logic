{:ns "clojure.core.logic.fd", :name "to-vals", :file "clojure/core/logic/fd.clj", :type :fn, :src "(defn to-vals [dom]\n  (letfn [(to-vals* [is]\n            (when is\n              (let [i (first is)]\n                (lazy-seq\n                 (cons (lb i)\n                       (if-let [ni (drop-one i)]\n                         (to-vals* (cons ni (next is)))\n                         (to-vals* (next is))))))))]\n    (to-vals* (seq (intervals dom)))))", :column 1, :line 685, :arglists ([dom])}