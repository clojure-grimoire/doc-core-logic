{:ns "clojure.core.logic", :name "solve", :file "clojure/core/logic.clj", :type :macro, :src "(defmacro solve [& [n [x] & goals]]\n  `(let [xs# (take* (fn []\n                      ((fresh [~x] ~@goals\n                         (fn [a#]\n                           (cons (-reify a# ~x) '()))) ;; TODO: do we need this?\n                       empty-s)))]\n     (if ~n\n       (take ~n xs#)\n       xs#)))", :column 1, :line 875, :macro true, :arglists ([& [n [x] & goals]])}