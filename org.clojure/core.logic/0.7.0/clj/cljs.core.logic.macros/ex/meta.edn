{:ns "cljs.core.logic.macros", :name "ex", :file "cljs/core/logic/macros.clj", :type :fn, :src "(defn ex\n  ([vs t a]\n     `(fresh [~@vs]\n        (== ~t ~a)))\n  ([vs t a exprs]\n     (if (fresh-expr? exprs)\n       `(fresh [~@vs]\n          (== ~t ~a)\n          ~exprs)\n       `(fresh [~@vs]\n          (== ~t ~a)\n          ~@exprs))))", :column 1, :line 268, :arglists ([vs t a] [vs t a exprs])}