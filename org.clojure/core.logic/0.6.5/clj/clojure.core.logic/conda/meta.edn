{:ns "clojure.core.logic", :name "conda", :file "clojure/core/logic.clj", :type :macro, :src "(defmacro conda\n  [& clauses]\n  \"Soft cut. Once the head of a clause has succeeded\n  all other clauses will be ignored. Non-relational.\"\n  (let [a (gensym \"a\")]\n    `(fn [~a]\n       (ifa* ~@(map (cond-clauses a) clauses)))))", :column 1, :line 1203, :macro true, :arglists ([& clauses])}